{
    "scripts": {
        "backend:init": "cd backend && ([ -d \".venv\" ] || uv venv) &&uv pip install -r requirements.txt",
        "backend:run": "cd backend && uv run main.py",
        "frontend:init": "cd frontend && bun install",
        "frontend:run": "cd frontend && bun run start",
        "frontend:dev": "cd frontend && bun run dev",
        "dev": "concurrently \"npm run backend:init && npm run backend:run\" \"npm run frontend:init && npm run frontend:dev\""
    }
}